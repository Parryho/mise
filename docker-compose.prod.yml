version: "3.8"

services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${PGUSER:-postgres}
      POSTGRES_PASSWORD: ${PGPASSWORD}
      POSTGRES_DB: ${PGDATABASE:-mise}
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./backups:/backups
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d mise"]
      interval: 10s
      timeout: 5s
      retries: 5

  app:
    build:
      context: .
      dockerfile: docker/Dockerfile
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
    expose:
      - "3000"
    volumes:
      - ./backups:/backups
      - recipe-uploads:/app/uploads/recipes
    environment:
      DATABASE_URL: postgresql://${PGUSER:-postgres}:${PGPASSWORD}@db:5432/${PGDATABASE:-mise}
      PORT: 3000
      NODE_ENV: production
      SESSION_SECRET: ${SESSION_SECRET}
    env_file:
      - .env

  nginx:
    image: nginx:alpine
    restart: unless-stopped
    depends_on:
      - app
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./docker/nginx/nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - certbot-conf:/etc/letsencrypt:ro
      - certbot-www:/var/www/certbot:ro

  db-backup:
    image: postgres:16-alpine
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
    volumes:
      - ./backups:/backups
    environment:
      PGUSER: ${PGUSER:-postgres}
      PGPASSWORD: ${PGPASSWORD}
      PGDATABASE: ${PGDATABASE:-mise}
    entrypoint: "/bin/sh -c 'while true; do sleep 86400; echo \"[$(date)] Running backup...\"; pg_dump -h db -U $$PGUSER $$PGDATABASE | gzip > /backups/mise_$$(date +%Y%m%d_%H%M%S).sql.gz && echo \"[$(date)] Backup done.\"; find /backups -name \"mise_*.sql.gz\" -mtime +14 -delete && echo \"[$(date)] Old backups cleaned.\"; done'"

  certbot:
    image: certbot/certbot
    volumes:
      - certbot-conf:/etc/letsencrypt
      - certbot-www:/var/www/certbot
    entrypoint: "/bin/sh -c 'trap exit TERM; while :; do certbot renew; sleep 12h & wait $${!}; done;'"

  # ── Monitoring (Optional) ──────────────────────────────────────────
  # Uncomment to add Prometheus + Grafana for metrics visualization.
  #
  # prometheus:
  #   image: prom/prometheus:latest
  #   restart: unless-stopped
  #   volumes:
  #     - ./docker/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
  #     - prometheus-data:/prometheus
  #   ports:
  #     - "9090:9090"
  #   depends_on:
  #     - app
  #   # prometheus.yml should contain:
  #   #   scrape_configs:
  #   #     - job_name: 'mise-app'
  #   #       scrape_interval: 15s
  #   #       metrics_path: /api/metrics
  #   #       static_configs:
  #   #         - targets: ['app:3000']
  #
  # grafana:
  #   image: grafana/grafana:latest
  #   restart: unless-stopped
  #   volumes:
  #     - grafana-data:/var/lib/grafana
  #   ports:
  #     - "3001:3000"
  #   environment:
  #     GF_SECURITY_ADMIN_PASSWORD: ${GRAFANA_PASSWORD:-admin}
  #   depends_on:
  #     - prometheus

volumes:
  pgdata:
  recipe-uploads:
  certbot-conf:
  certbot-www:
  # prometheus-data:
  # grafana-data:
